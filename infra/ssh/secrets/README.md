# SSH keys (local only)

Place here before running `setup-vps-access.ps1`:

- `hetzner_deploy` — private key (OpenSSH format)
- `hetzner_deploy.pub` — public key

These filenames match the script defaults. Do not commit this directory’s key files.
